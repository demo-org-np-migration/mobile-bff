apiVersion: v1
kind: Service
metadata:
  name: mobile-bff
  labels:
    {{- include "mobile-bff.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "mobile-bff.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
